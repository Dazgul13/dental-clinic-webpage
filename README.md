# Dental Clinic Website (Vue + Vite)

Mobile-first demo site using Vue 3 and Vite. Glassmorphism styling, services cards, contact form, and Leaflet map.

Quick start

1. Install dependencies

```bash
npm install
```

2. Run dev server

```bash
npm run dev
```

Open the URL shown by Vite (usually http://localhost:5173).
